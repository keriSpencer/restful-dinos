<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Dinosaurs!</title>
    <link rel="stylesheet" href="screen.css">
  </head>
  <body>
    <h1>Dinosaurs!</h1>
    <h3><a href="/new">Add a New Dinosaur</a></h3>
    <div class="container">
      {{#dinos}}
        <div class="singleDino">
          <img src="{{img}}" alt="{{name}}">
          <a href="/api/dinosaurs/{{id}}">{{name}}</a>
        </div>
      {{/dinos}}
    </div>
  </body>
</html>
